apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: health-api
spec:
  rules:
  - host: health.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: health-api
            port:
              number: 80